/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

// next.config.js
module.exports = {
  // Enable the `src` directory for Next.js pages
  experimental: {
    appDir: true,
  },
};
